<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <style>
        body {
            background-color: #eee;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .receipt {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 2px 2px 8px rgba(54,58,64,.2);
                        height: 450px;
            width: 500px;
            margin-top: 50px;
            margin-bottom: 50px;
        }

        .receipt img {
            width: 300px;
        }

        .receipt h4 {
            margin-top: 10px;
            margin-bottom: 20px;
        }

        .receipt hr {
            margin-top: 20px;
            margin-bottom: 20px;
        }

        .receipt .footer {
            margin-top: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .receipt .thanks {
            font-weight: bold;
        }

        .qr-code {
            position: relative;
            max-width: 150px; 
        }

        .copy-button {
            cursor: pointer;
            padding: 5px 10px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 8px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="d-flex justify-content-center row">
            <div class="col-md-10">
                <div class="receipt">
                    <img src="../public/images/AITAM Logo.png" alt="Amazon Logo">
                    <h4>Your complaint is Registered!!</h4>
                    <hr>
                    <!-- Insert reference ID dynamically -->
                    <div class="d-flex justify-content-between mt-1">
                        <span style="color:red; font-weight:400;">Reference ID:</span> <span id="refId"><%= refId %></span>
                        <button class="copy-button" onclick="copyToClipboard()">Copy</button>
                    </div>
                    <br>
                    <br>
                    <span class="mt-3 text-black-50 fs-15">Your complaint should be verified, and our team will resolve it soon.</span>
                    <hr>
                    <div class="footer">
                        <div class="thanks">
                            <span class="font-weight-bold">Thanks </span>
                            <span>Aitam team</span>
                        </div>
                        <br>
                        <div>
                            <span class="font-weight-bold">Need Help?</span>
                            <span>Call - 989898989</span>
                        </div>
                    </div>
                    <div style="display: flex; align-items: center; justify-content: space-between;">
                        <span style="color:navy;" id="dateTime"></span>
                        <img class="qr-code" src="<%= qrCodeUrl %>" alt="QR Code">
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function copyToClipboard() {
            var refId = document.getElementById('refId');
            var range = document.createRange();
            range.selectNode(refId);
            window.getSelection().removeAllRanges();
            window.getSelection().addRange(range);
            document.execCommand('copy');
            window.getSelection().removeAllRanges();
            alert('Reference ID copied to clipboard: ' + refId.innerText);
        }

        // Function to display current date and time
        function displayDateTime() {
            var now = new Date();
            var dateTimeElement = document.getElementById('dateTime');
            dateTimeElement.textContent = now.toLocaleString();
        }

        // Call the function to display current date and time
        displayDateTime();
    </script>
</body>
</html>
